public class Result
{
    private bool _status;

    public Result(bool status)
    {
        _status = status;
    }

    public Result(bool status)
    {
        _status = status;
    }

    public bool Status => _status;
}
